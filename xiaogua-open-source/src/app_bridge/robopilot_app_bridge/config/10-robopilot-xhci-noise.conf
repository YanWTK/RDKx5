# The X5 xHCI driver emits this warning for every affected reSpeaker packet.
# Keep all other kernel messages and discard only this confirmed endpoint storm.
if ($syslogfacility-text == "kern" and $msg contains "xhci-hcd.2.auto: WARN: buffer overrun event for slot 5 ep 2") then stop
