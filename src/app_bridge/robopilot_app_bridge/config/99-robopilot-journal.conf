[Journal]
# Kernel messages remain available in /var/log/kern.log through rsyslog.
# Avoid processing the same high-rate USB stream a second time in journald.
ReadKMsg=no
SystemMaxUse=256M
RuntimeMaxUse=64M
RateLimitIntervalSec=30s
RateLimitBurst=1000
